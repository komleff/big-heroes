---
name: verify
description: Быстрый healthcheck проекта — сборка + тесты одной командой
user-invocable: true
---

# Verify — healthcheck проекта

Запусти сборку и тесты, выведи результат.

```bash
npm run build && npm run test
```

Если что-то упало — покажи ошибки и предложи исправление.
